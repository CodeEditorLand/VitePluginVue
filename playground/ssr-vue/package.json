{
	"name": "@codeeditorlandjs/test-ssr-vue",
	"scripts": {
		"build": "npm run build:client && npm run build:server",
		"build:client": "vite build --ssrManifest .vite/ssr-manifest.json --outDir dist/client",
		"build:noExternal": "npm run build:client && npm run build:server:noExternal",
		"build:server": "vite build --ssr src/entry-server.js --outDir dist/server",
		"build:server:noExternal": "vite build --config vite.config.noexternal.js --ssr src/entry-server.js --outDir dist/server",
		"debug": "node --inspect-brk server",
		"dev": "node server",
		"generate": "vite build --ssrManifest .vite/ssr-manifest.json --outDir dist/static && npm run build:server && node prerender",
		"serve": "NODE_ENV=production node server"
	},
	"dependencies": {
		"@codeeditorlandjs/test-example-external-component": "file:example-external-component",
		"pinia": "2.3.0"
	},
	"devDependencies": {
		"@codeeditorlandjs/plugin-vue-jsx": "workspace:*",
		"@codeeditorlandjs/test-dep-import-type": "link:dep-import-type",
		"compression": "1.7.5",
		"serve-static": "1.16.2"
	}
}
